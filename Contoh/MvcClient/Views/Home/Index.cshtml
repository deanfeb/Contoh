@using Microsoft.AspNetCore.Authentication

<h2>Claims</h2>

<a href="https://localhost:44394/home/delunit">Del Unit</a>
<a href="https://localhost:44394/home/GetUnit">Get Unit id 1</a>
<a href="https://localhost:44394/home/delunit">Add Unit</a>
<br />
<br />
<br />
<br />

<dl>
    @foreach (var claim in User.Claims)
    {
        <dt>@claim.Type</dt>
        <dd>@claim.Value</dd>
    }
</dl>

<h2>Properties</h2>

<dl>
 @*   @foreach (var prop in (await Context.AuthenticateAsync()).Properties.Items)
    {
        <dt>@prop.Key</dt>
        <dd>@prop.Value</dd>
    }*@
</dl>